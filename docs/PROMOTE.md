# 📣 推广计划 —— 把作品变成影响力

> 目标：让"DeepSeek Harness 第三方控制面"成为你的标签。
> 节奏：一次性做完第 1-2 步（半天），然后每周 1-2 篇内容。

## 第 1 步：发布到 GitHub（✅ 已完成 2026-08-18）

> 仓库：https://github.com/aazjzkskkw/agent-workbench （已推送，SSH 443 通道）
> 剩余待办：① 仓库 About 里加描述和 topics ② README 加截图 ③ 第一次发布 tag

> 公钥（~/.ssh/id_ed25519.pub 的内容）：
> ```
> ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIIMAkLztEnsBpiZ+k2wbo8Bz34E1zOskJpA2FkxRRup7 wsy-github
> ```

4. 发布后补充仓库页面：
   - README 顶部加 2-3 张截图（演示视频的截图即可）
   - 在 GitHub 仓库 Settings → About 里写描述、加 topics：`deepseek-harness` `agent` `control-plane` `llm` `desktop-pet`

## 第 2 步：录 3 分钟演示视频（手机录屏即可）

脚本（照着念）：
```
0:00-0:30  一句话介绍：这是 DeepSeek Harness 的多后端 agent 控制面
0:30-1:00  新建 runtime → 发任务 → 看事件流实时滚动
1:00-1:30  审批：bash 被拦 → 点"允许本会话全部" → 任务继续
1:30-2:00  桌宠表演：思考 → 调工具 → 完成欢呼；拖一个文件给它
2:00-2:30  导出会话 + 看统计面板（token/成本）
2:30-3:00  一句话总结 + GitHub 地址
```

**加长版（4-5 分钟，录长视频/直播用）**，补充：
```
+3:00  定时任务：设置"每 5 分钟自动跑日报"→ 演示它自动触发（桌宠"收到新任务！"）
+3:30  对话回放：点 ⏪ 回放 → 拖滑块回看 agent 每一步
+4:00  多后端：新建 claude/gemini 运行时，同一界面切换（已装哪个展示哪个）
+4:30  成本告警：把阈值调到 ¥0.01 演示 toast 弹警告
```

## 第 3 步：写技术博客

- ✅ **第一篇已出初稿**：`docs/blog-01.md`《我给 DeepSeek Harness 搭了一个多后端 agent 控制面》
  ——发布前把文首的 `[你的名字]` / `[链接]` 占位符替换掉，按自己语气顺一遍即可发。

其他选题（按热度排序，全部用真实素材）：
1. **《DeepSeek Harness SDK 协议考古：审批通道不存在，所以我用文件桥造了一个》** —— 技术深度
2. **《一个编程小白用 AI 做出了 agent 工作台：我的踩坑记录》** —— 故事性，流量款
3. **《我把六个 agent 平台统一进一个控制面》** —— 多后端抽象拆解

每篇结构：背景问题 → 我怎么做 → 踩了什么坑 → 结果 → 下一步。500-1500 字，配 2-3 张截图。

## 第 4 步：社区渠道（每周 1-2 次，每次 15 分钟）

| 渠道 | 发什么 |
|---|---|
| 掘金 / 知乎 | 博客 + 截图，标签：AI、Agent、开源 |
| B站 | 3 分钟演示视频，标题带"DeepSeek""桌宠""agent" |
| GitHub Discussions（deepseek-harness 仓库） | 在 SDK 相关问题下回答（先问后答，攒信誉） |
| X/Twitter | 发 gif 演示（桌宠动画很抓眼球），tag #deepseek #agent |

## 第 5 步：持续运营（1 个月后）

- 收集 GitHub issue → 修 1-2 个 → 再写一篇《根据用户反馈我改了这些》
- 看 README 的 star 数：>30 就写《我的开源项目一个月后》复盘文
- 把"第一个 DeepSeek Harness 第三方控制面"这个定位反复讲

## 度量（什么算成功）

- 第 1 个月：仓库发布 ✓、演示视频 ✓、1 篇博客 ✓
- 第 3 个月：star > 50，3 篇博客，有人在 issue 里问问题（就是被看见了）
- 目标不是"火"，是"**有可展示的痕迹**"——面试时你能甩出：仓库 + 视频 + 博客
