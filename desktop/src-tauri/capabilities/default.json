{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window: core defaults only (a pure webview shell).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
